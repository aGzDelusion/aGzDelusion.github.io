#include <iostream>
using namespace std;

const int TEMP = 30;
bool airConditioner(int temp, bool &acOn)
  return (acOn >= TEMP? true : false);

int main()
{
  int temperature = 0;
  bool isAirConditioner = false;
  cin >> X;

  airConditioner(X, isAirConditioner);

case(X){
X

}
  if(isAirConditioner)
    cout << "The current temperature is " << X << ", so the Air Conditioner will turn on" << endl;

  else
    cout << "The current temperature is "<< X << ", so the Air Conditioner will NOT turn on" << endl;

  return 0;
}